<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <% if(allMovies.length > 0){ %>
    <% for(let i=0; i<allMovies.length; i++){ %>

    <div>
        <h2><%= allMovies[i].title %></h2>

        <form method="POST" action="/movies/like">
            <input type="hidden" name="course_id" value="<%= allMovies[i].id %>">
            <button class="btn btn-lg btn-warning text-white"><span>LIKE</span></button>
        </form>

        <p><%= allMovies[i].likeCount %> likes</p>
        <p><%= allMovies[i].description %></p>
    </div>

    <% } %>
    <% } else { %>

    <div>
        There is no movie in the database yet
    </div>
    <% } %>

</body>

</html>